# Student Management System - Architecture Documentation

## C1 - Context Diagram
System Overview

Student Management System เป็นระบบจัดการข้อมูลนักศึกษาแบบ REST API
พัฒนาโดยใช้ Node.js, Express และ SQLite

Actors

* นักศึกษา / ผู้ดูแลระบบ (เรียกใช้งานผ่าน Postman หรือ Web Client)

External Systems

* SQLite Database
```
┌───────────────┐
│   User / API  │
│   Client      │
└───────┬───────┘
        │ HTTP Request
        ▼
┌──────────────────────────┐
│ Student Management API   │
│ (Node.js + Express)      │
└────────────┬─────────────┘
             │ SQL
             ▼
        ┌──────────┐
        │ SQLite   │
        └──────────┘

```

---

## C2 - Container Diagram (Layered Architecture)
ระบบถูกออกแบบด้วย Layered Architecture (3-Tier)
```
┌─────────────────────────────────────┐
│     Presentation Layer              │
│  ┌──────────────────────────────┐   │
│  │ Routes → Controllers         │   │
│  │ (HTTP Handling)              │   │
│  └──────────────────────────────┘   │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│     Business Logic Layer            │
│  ┌──────────────────────────────┐   │
│  │ Services → Validators        │   │
│  │ (Business Rules)             │   │
│  └──────────────────────────────┘   │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│     Data Access Layer               │
│  ┌──────────────────────────────┐   │
│  │ Repositories → Database      │   │
│  │ (SQL Queries)                │   │
│  └──────────────────────────────┘   │
└──────────────┬──────────────────────┘
               │
               ▼
          ┌──────────┐
          │  SQLite  │
          └──────────┘
```

---

## Responsibilities of Each Layer

### 1️⃣ Presentation Layer

โฟลเดอร์: src/presentation

ประกอบด้วย

* routes/

* controllers/

* middlewares/

หน้าที่

* รับ HTTP Request

* Validate request format

* เรียก Business Layer

* ส่ง HTTP Response กลับ client

ตัวอย่างไฟล์
 
* studentRoutes.js

* studentController.js

* errorHandler.js

---

### 2️⃣ Business Logic Layer

โฟลเดอร์: src/business

ประกอบด้วย

* services/

* validators/

หน้าที่

* ตรวจสอบ business rules

* ตรวจสอบความถูกต้องของข้อมูล

* ประมวลผล logic

* เรียก Data Layer

ตัวอย่างไฟล์

* studentService.js

* studentValidator.js

---

### 3️⃣ Data Access Layer

โฟลเดอร์: src/data

ประกอบด้วย

* repositories/

* database/

หน้าที่

* ติดต่อฐานข้อมูล

* เขียน SQL Queries

* CRUD operations

ตัวอย่างไฟล์

* studentRepository.js

* connection.js

---

## 🔄 Data Flow (Request → Response)

```
Client (Postman)
   │
   ▼
studentRoutes.js
   │
   ▼
studentController.updateGPA()
   │
   ▼
studentService.updateGPA()
   │
   ▼
studentRepository.updateGPA()
   │
   ▼
SQLite Database
   │
   ▼
studentRepository
   │
   ▼
studentService
   │
   ▼
studentController
   │
   ▼
Client Response (JSON)

```

---

## 🎯 Architecture Benefits

ข้อดีของ Layered Architecture

1.แยกหน้าที่ชัดเจน (Separation of Concerns)

2.แก้ไขง่าย ไม่กระทบทั้งระบบ

3.รองรับการขยายระบบในอนาคต

4.ทดสอบแต่ละ layer ได้ง่าย

5.โค้ดอ่านง่าย เป็นมาตรฐานอุตสาหกรรม

---

## ✅ Conclusion

Student Management System ถูกออกแบบโดยใช้ Layered Architecture เพื่อให้โครงสร้างระบบมีความเป็นระเบียบ รองรับการพัฒนาในระยะยาว และเป็นไปตามหลัก Software Architecture ที่ดี

